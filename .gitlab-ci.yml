# Publishes the site with GitLab Pages on every push to the default branch.
pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r index.html styles.css script.js assets public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
